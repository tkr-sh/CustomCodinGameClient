def test():
    assert (1).isprime==False, "1 is not prime"
    assert (2).isprime==True, "2 is prime"
    assert (3).isprime==True, "3 is prime"
    assert (4).isprime==False, "4 is not prime"
    assert (5).isprime==True, "5 is prime"
    assert (7).isprime==True, "7 is prime"
    assert (9).isprime==False, "9 is not prime"
    assert (11).isprime==True, "11 is prime"